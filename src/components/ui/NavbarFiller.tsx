import { STYLE } from '@/constants';

export default function NavbarFiller() {
  return (
    <div
      style={{
        minHeight: STYLE.NAVBAR_HEIGHT,
        minWidth: '100%',
      }}
    ></div>
  );
}
